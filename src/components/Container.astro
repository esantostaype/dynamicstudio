---
interface Props {
	className?: string
}
const { className } = Astro.props
---

<section class={`px-8 md:px-10 xl:px-0 mx-auto w-full lg:max-w-5xl xl:max-w-7xl ${ className }`}>
  <slot/>
</section>