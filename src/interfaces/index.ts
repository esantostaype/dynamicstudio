export interface Nav {
  label: string
  path: string
  active: boolean
}

export interface Service {
  slug: string
  title: string
  image: string
}

export interface Project {
  slug: string
  title: string
  image: string
  category: string
}